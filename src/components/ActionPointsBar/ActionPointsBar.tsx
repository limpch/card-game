import { FC } from "react"
import classes from "./styles.module.scss"

interface ActionPointsBarProps {}

export const ActionPointsBar: FC<ActionPointsBarProps> = ({}) => {
	return <div className={classes.container}>ActionPointsBar</div>
}
