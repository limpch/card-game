import { FC } from "react"
import classes from "./styles.module.scss"

interface ShieldsBarProps {}

export const ShieldsBar: FC<ShieldsBarProps> = ({}) => {
	return <div className={classes.container}>ShieldsBar</div>
}
