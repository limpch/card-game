import { FC } from "react"
import classes from "./styles.module.scss"

interface CharactersPageProps {}

export const CharactersPage: FC<CharactersPageProps> = ({}) => {
	return <div className={classes.container}>CharactersPage</div>
}
