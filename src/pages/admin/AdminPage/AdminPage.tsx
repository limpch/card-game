import { FC } from "react"
import classes from "./styles.module.scss"

interface AdminPageProps {}

export const AdminPage: FC<AdminPageProps> = ({}) => {
	return <div className={classes.container}>AdminPage</div>
}
