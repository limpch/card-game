import { FC } from "react"
import classes from "./styles.module.scss"

interface AdminEditCardPageProps {}

export const AdminEditCardPage: FC<AdminEditCardPageProps> = ({}) => {
	return <div className={classes.container}>AdminEditCardPage</div>
}
