import { FC } from "react"
import classes from "./styles.module.scss"

interface DeckStoreLayoutProps {}

export const DeckStoreLayout: FC<DeckStoreLayoutProps> = ({}) => {
	return <div className={classes.container}>DeckStorLayout</div>
}
